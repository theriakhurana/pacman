{
  "name": "pacman-web",
  "version": "1.0.0",
  "private": true,
  "description": "Classic PAC-MAN implemented with vanilla JS and HTML canvas, with multiple levels.",
  "scripts": {
    "dev": "npx serve . -l 5050",
    "snapshots": "node scripts/generate-snapshots.js"
  },
  "devDependencies": {
    "puppeteer": "^22.15.0",
    "serve": "^14.2.3",
    "serve-handler": "^6.1.5"
  }
}
